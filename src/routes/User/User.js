const express = require('express');

const route = express.Router();

// GET REQ --> /user/             GET CURRENT USER

route.get('/', (req,res) => {

})

// PUT REQ --> /users/             UPDATE CURRENT USER

route.put('/', (req,res) => {

})

module.exports = route;